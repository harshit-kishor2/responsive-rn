import { rpFont, rpHeight, rpInitialize, rpInstance, rpNormalize, rpWidth } from "./src/responsiveClass";

export default {
    rpFont,
    rpWidth,
    rpHeight,
    rpNormalize,
    rpInitialize,
    rpInstance
}